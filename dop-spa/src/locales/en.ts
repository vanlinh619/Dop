const en: typeof import('./vi').default = {
    accessDenied: {
        consentDenied: {
            title: 'Consent denied',
            description: 'You denied the consent request, so we couldn’t complete the sign-in process.',
            actionRetryLogin: 'Retry sign in'
        }
    }
}

export default en